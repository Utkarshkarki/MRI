# Base Configuration mapping for Phase 1
gpu:
  mixed_precision: true

model:
  num_classes: 4
  num_ensemble_heads: 3
  dropout_rate: 0.5
  
training:
  batch_size: 32 # Scaled down structurally to ensure safe memory on typical instances
  num_epochs: 50
  learning_rate: 1e-3

uncertainty:
  mc_passes: 15 # Reduced heavily to preserve real-time Streamlit UI speeds natively
  anomaly_threshold: 0.25
